# SPEC 与 PLAN 生成过程

本文件只记录实际发生的讨论、验证和修订。它不补造未发生的对话、agent 输出或人工修改。

## 1. 使用方式与参与者

- **主开发 agent：** Codex，使用 Superpowers 的 brainstorming 流程组织需求、规约和计划。
- **规约辅助：** 结构化输出技能用于定义可验证的 action JSON；OpenAI Docs/智增增文档用于确认 Responses endpoint 形态。
- **人类负责人：** 项目作者。负责项目方向、风险取舍、预算、平台、最终反思和对外发布确认。
- **异构冷启动 agent：** 尚未执行。必须在 P02 使用与 Codex 类型不同的新会话，且只提供本仓库的 `SPEC.md` 和 `PLAN.md`。

## 2. 关键 brainstorming 迭代

### 迭代一：从作业要求到可评分的核心

**背景。** 阅读通用要求与 Harness 专项要求后，核心歧义是“做很多 agent 功能”还是“选择一个可确定性验证的机制做深”。

**agent 的关键追问/建议。** 将主贡献限定为治理与 HITL，而不是把六个维度都做成提示词或薄封装；所有机制必须在 mock LLM 下可单测。

**人类决策。** 以 Coding Agent Harness 为项目类型，追求高分，接受 CLI 优先和治理深度优先的建议。

**影响。** SPEC 将路径围栏、策略、审批/恢复、审计定为重点；反馈、记忆、配置仍保留最小可运行实现。

### 迭代二：十小时约束与分发形态

**背景。** 人类提出约十小时总时长、每个小进度中文 commit、希望参考成熟开源项目的思路，且最终不做 UI。

**agent 的关键追问/建议。** 课程要求 SPEC/PLAN 和异构冷启动验证在实现代码之前完成，因此不能把全部十小时都当作编码；为避免 UI 挤占机制测试，采用助教允许的 CLI + Release 路线。对参考开源项目只借鉴架构并记录来源，不能复制源码或伪造历史。

**人类决策。** 确认不开发 UI，接受“CLI-only + GitHub Release”的交付方式，并要求真实的增量 Git 历史。

**影响。** 计划将冷启动验证设为 P02，Release tarball 取代 WebUI 部署；每个任务保留红/绿/重构中文 commit。

### 迭代三：安全、费用和托管选择

**背景。** 人类提供智增增 Responses endpoint、期望使用 `gpt-5.4-mini`，并要求安全封装。

**agent 的关键追问/建议。** 真实 API Key 不能进入聊天、日志、文件或 Git，已提示立即轮换；测试/CI 必须只用 mock。推荐 macOS Keychain、70 元应用侧预算上限和保守策略：危险命令要求审批，越界/发布命令拒绝。

**人类决策。** 选择 GitHub 公开主仓库，确认保守策略，设定 70 元总预算，并要求先自动检查异构 agent；提供姓名/学号供最终独立提交模板使用。

**影响。** 使用 `se-project` Keychain 服务，默认模型为 `gpt-5.4-mini`；GitHub 仓库创建为 `K11467/sentinel-coding-harness`；不在 CI 中配置真实 Key。

## 3. AI 建议的采纳与修正

| 建议 | 人类处理 | 原因 |
| --- | --- | --- |
| CLI-first，不做 UI | 采纳 | 助教允许 Release；有限时间应优先可验证的 Harness 内核 |
| 治理/HITL 作为深度维度 | 采纳 | 最符合“机制必须是代码、mock 下仍可验证” |
| 自动生成最终反思 | 拒绝 | 课程规定反思须由学生本人撰写；AI 只能辅助整理和润色 |
| 使用已在聊天中出现的 API Key | 拒绝 | 已暴露，不安全；必须轮换后仅在本机 Keychain 录入 |
| 放宽高风险命令以提升演示自由度 | 拒绝 | 安全边界和可预测性优先于自由执行 |

## 4. 对 brainstorming 的初步反思

它的价值在于先迫使项目收敛到一个能通过 mock 证明的核心机制，并明确“不做 UI”是范围控制而不是功能缺失。它的局限是默认流程会消耗大量对话回合；本项目时间有限，因此把每一轮收敛为可审查的明确决定，并将冷启动验证作为检查规约是否遗漏隐性假设的客观步骤。

## 5. 冷启动验证记录（P02 后补充）

P02 将补充：异构 agent 类型与版本、无历史会话的启动方式、给出的仅有文件、其停下询问的位置、误读与产出差距、修订前后的关键 diff，以及人类对每项建议的处理。此节现在不宣称已完成。
