# 课程反思

## 项目范围的取舍

一开始我想过要不要做 WebUI。后来结合时间和作业要求，我决定只做 CLI。助教允许 CLI 项目用 GitHub Release 发布，所以我把主要时间放在 Harness 的执行链路上。最后项目能从命令行完成配置检查、凭据管理、任务运行、审批、恢复和审计，也有可以直接运行的离线 demo。

我比较重视模型输出之后的处理。模型输出不会直接作为命令执行，必须先转换成受限制的 action，再经过字段校验、路径检查、策略判断和审批。这样做会让实现复杂一些，但能把每一层的责任分开。遇到错误时，程序可以明确停止、要求审批或返回受控错误，而不是继续执行。

## 一次印象较深的修正

文件工具最初已经能拦截绝对路径、`..` 和普通的符号链接。审查时又发现了一个问题：路径检查结束后，如果父目录被另一个进程替换成外部符号链接，原来的实现仍可能访问工作区外的文件。

我先补了会触发这个问题的测试，再调整文件操作方式。修改后，程序会在受控目录中复查路径，并对最终文件使用额外限制。之后又发现把接近 256 KiB 的写入内容直接放进子进程参数，在 macOS 上可能超过参数长度限制。这个问题也是先加测试，再把内容改为通过标准输入传递。两次修正都保留了测试失败、修复和测试通过的提交记录。这个过程让我认识到，普通路径测试通过以后，还需要检查竞态和边界大小等情况。

## 对 AI 辅助开发的看法

本项目使用了多个 AI agent 协助实现和审查，但我没有把它们的输出直接当作结论。开始实现前，Cursor 的冷启动结果指出了 action 字段、会话状态和测试脚手架顺序的问题。我根据这些问题补充了 SPEC 和 PLAN，确认后才进入实现。

后续开发中，agent 分别处理协议、工具、策略、审批、审计、配置和 CLI。安全审查还发现了命令解释器绕过、审批持久化不完整、Keychain 清理失败和 Provider 地址校验不严等问题。对于这些问题，我要求补充测试并重新验证，而不是只修改实现。最终离线测试、GitHub CI、打包和安装后的 demo 都通过，但真实 Provider 没有被当成测试结果使用。

## 当前限制和后续计划

这个 Harness 适合本地的小型编码任务。它可以限制通过 Sentinel 发起的文件和命令操作，但无法处理拥有同等本机权限的恶意程序。命令超时会结束受控进程组，也不能覆盖所有主动脱离进程组的情况。配置中的预算上限只能限制 Harness 自己的策略，不能完全替代 Provider 的实际计费。

如果还有时间，我会优先补充隔离仓库上的端到端测试，重点验证暂停、审批、恢复和审计之间的完整过程。其次会改进审计信息的展示，方便用户在批准动作前更快看清风险和影响范围。

## 诚信说明

提交材料中的测试、CI、打包和 Release 结论都有实际记录。真实模型调用没有被写成已经通过的测试。项目参考了公开开源项目的设计思路，相关来源已写入 `REFERENCES.md`。提交前我会再次检查反思内容和提交文件，确认它们与实际过程一致。
