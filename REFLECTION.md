# 课程反思

> 本文先由 AI 根据项目的真实过程记录整理成草稿，已由项目作者负责校对、补充和定稿；提交前应确认每一处表述都符合自己的实际经历。

## 1. 我承担的设计决策

这个项目最开始让我纠结的是：要不要花大量时间做一个看起来更“完整”的 WebUI，还是把时间放在 Coding Agent 真正容易失控的部分。最后我选择了 CLI-only 的交付方式，并通过 GitHub Release 提供可安装版本。这个选择不是因为 UI 不重要，而是课程作业的核心是 Harness 的机制是否能被验证。时间有限时，一个能清楚说明输入、决策、执行和停止条件的 CLI，比一个界面好看但安全边界很薄的 Demo 更有说服力。

我把治理和 HITL 审批作为项目最深入的一条线。模型不能直接输出一段 shell 字符串后立刻执行，而是先返回受限 action，再经过本地 schema 校验、路径围栏、策略判定和审批记录。比如写文件时会检查工作区真实路径、符号链接和大小；命令使用参数数组加 `shell:false`；像递归删除、`git push`、提权和发布等动作会被直接拒绝或转为人工审批。这个取舍让我认识到，Agent 的能力不只取决于模型会不会“想”，还取决于系统能否在模型出错时稳定地拒绝、暂停和解释。

## 2. 一次真实的迭代与修正

我印象最深的是工作区文件工具的安全修正。最初版本已经能拒绝 `../`、绝对路径和静态的外部符号链接，普通测试也都能通过。但审查时发现，仅仅在操作前 `realpath` 并不够：如果检查完成后父目录被替换成指向外部的符号链接，仍可能出现 TOCTOU（检查与使用之间的竞态）问题。

这个问题让我意识到“测试通过”不等于“机制已经可靠”。后续先补了能稳定复现父目录替换的红测，再把实际文件操作改为在经过复验的受控目录中执行，同时结合文件描述符和 `O_NOFOLLOW` 限制最终符号链接。之后又发现 256 KiB 边界内容放进子进程参数会触发系统的参数长度限制，于是把内容传递改为标准输入，并新增恰好 256 KiB 内容的回归测试。这个过程虽然花了时间，但保留了红测、修复和绿测的中文提交记录，也让我更理解安全边界需要针对错误路径和边界条件反复验证。

## 3. 协作与过程

本项目使用了 AI 辅助开发，但我没有把它当成“自动生成后直接提交”的黑盒。项目开始前，我先根据作业要求确定了 CLI-only、70 元预算上限、默认模型和不在 CI 中使用真实 Key 等边界；异构的 Cursor 冷启动也暴露了早期规格中的问题，例如测试脚手架顺序、action 字段和会话状态契约不够明确。之后这些问题被写回 SPEC 和 PLAN，再开始实现。

开发过程中，不同子任务按红测、最小实现、审查修复的节奏推进。对工作区围栏、命令执行、策略配置、审批原子性、Keychain 清理和 Provider URL 钉扎等模块，审查发现的问题没有被掩盖，而是作为新的测试和中文提交保留下来。我的责任是确认项目范围、审查结论和最终交付不夸大测试结果。例如真实 Provider 没有在 CI 中调用，Keychain 真机用例也明确标注为 opt-in，而不是把未执行的测试写成已通过。

## 4. 局限与下一步

当前项目适合本地、小范围、可控的编码任务，不适合直接当作完全自主的软件工程系统。文件围栏已经尽量处理了路径、符号链接和部分竞态，但它并不等价于防御拥有同等本机权限的恶意进程；命令超时会清理受控进程组，也不能保证处理故意逃逸的任意后台进程。`maxCostCny` 是 Harness 的保守预算设置，不能替代第三方 Provider 的真实计费控制。

如果再给我一周时间，我会优先做两件事。第一是加入更多真实但隔离的端到端测试，例如用临时仓库验证暂停、批准、恢复和审计链路，而不是只依赖 mock。第二是把审计记录做成更方便人工复核的报告，并增加对更多平台的适配。不过这些扩展仍应建立在现有的“先验证再执行、先脱敏再记录”原则上，而不是为了增加功能而放宽边界。

## 5. 诚信声明

我确认本提交中的过程记录、测试结论和 Release 信息只陈述实际发生的工作：离线测试、构建、打包、安装 smoke test 和 GitHub CI 均有对应证据；真实模型调用没有被伪造成测试结果。项目参考了开源项目的公开设计思路，并在 `REFERENCES.md` 中说明，没有把外部项目作为运行时依赖或直接复制其核心实现。提交前我会再次检查本文件，使其与自己的实际参与、理解和判断保持一致。
